# 项目协作规则

## 独立任务工作区

- 本项目主目录为 `/Users/jerryszz/Desktop/实习/Empact/empactchinaOfficialWeb`，用于同步 `main` 和提供日常开发预览，不作为多个任务共用的实现目录。
- 每个任务开始修改文件前，先更新远端引用，再从最新 `origin/main` 创建独立的 `agent/<task-name>` 分支和 Git worktree；任务已拥有独立 worktree 时继续使用该目录。后续编辑、检查、提交和推送都显式指定该 worktree 的路径。
- 不同任务不得共用 worktree、接手其他任务的分支，或在其他任务的目录中切换分支、暂存、提交。并行任务可以独立开发；修改同一处产生的冲突在各自任务分支中解决。
- 完成检查后提交、推送并创建 PR；经授权合并后，核对主目录的未提交文件、素材、业务数据和运行进程，再安全同步最新 `main`。只清理本任务已合并且没有独有改动、数据或活跃进程依赖的 worktree 和本地及远端分支。
- 对仍在主目录进行的旧任务，先确认改动归属并迁移到各自 worktree，核对迁移完整后再清理原改动；不得覆盖、丢弃或代为提交其他任务的工作。启动服务前核对实际工作目录、当前分支与提交，不能只检查页面是否返回 200。

## 服务器操作

- 本项目服务器及阿里云资源的查询、配置、部署、日志排查和服务管理，优先使用 CLI、SSH、官方 MCP 或 OpenAPI；服务器内命令优先通过 SSH 或云助手 API 执行。
- 所需命令行或 API 通道不可用时，先排查连接、认证和权限。只有这些方式确实无法完成、必须使用 Browser Control、Computer Use、浏览器自动化或控制台网页终端时，才可在说明具体原因和拟执行操作、取得用户明确确认后使用。
- 操作后通过命令退出码、API 执行结果及必要的服务状态或 HTTP 检查确认实际效果；异步命令必须查询最终执行结果。
- 用户要求正式站点交付或处理部署故障时，合并后须跟进对应 main CI 和 `Deploy production` 的最终结果，再核对公开 `release.json` 的 `codeRevision` 及本次受影响页面。不能把 PR 合并或 CI 成功当成上线完成；失败时先按 `docs/planning/operations/automatic-deployment.md` 排查，不能盲目重跑或放宽结构变更保护。

## 统一开发预览

- 用户要求打开、重启或查看日常本地预览时，固定在上述主目录根目录运行 `npm run dev`。日常预览不改用 `dev:cms`、`serve`、`serve:preview`、`serve:workspace`、直接运行 Astro 或临时自建服务。
- 本项目所有本地开发、预览和验收服务统一使用 `http://127.0.0.1:4321`，对用户提供的本地链接也必须使用 4321。
- 任务 worktree 需要浏览器验收时，先协调 4321 的使用权，再临时切换到该 worktree 根目录运行 `npm run dev`；验收完成或失败后均停止任务服务并恢复主目录的开发预览。不得为预览将任务改动复制到主目录或切换主目录分支。独立 worktree 不代表可以同时占用共享端口。
- 启动前检查 4321 的监听进程、所属工作目录、当前分支与提交。符合当前预览或验收目标的开发服务可以复用；若为本项目旧目录或其他模式的服务，确认没有其他任务正在使用后，先停止该进程，再用 `npm run dev` 启动。不要直接结束其他项目或其他任务正在使用的进程。
- 在服务所属目录根目录运行 `npm run dev:status`、`npm run dev:logs`、`npm run dev:stop`，仅用于查看和停止 `npm run dev` 启动的 Astro 开发预览。`npm run dev` 使用现有后台启动机制，验证成功后即可结束任务，不持续等待开发进程退出；日常代码修改由热更新处理，无需每次停止服务。
- 禁止因端口占用自动递增或另开 4322、4323 等端口。无法安全释放 4321 时，说明占用情况，不得擅自换端口。
- CMS、静态预览及生产服务命令仅用于明确要求的后台或发布工作，以及必要的自动化验证。测试需要独占 4321 时，先协调使用权，再停本项目的开发预览；无论测试成功或失败，都清理测试服务并恢复主目录根目录的 `npm run dev`，不得把测试服务留作用户的预览入口。
- 上述其他模式的服务通过其启动终端或测试清理流程停止；需要手动停止时，先核对监听 PID 及所属工作目录，再停止该服务进程。切回开发预览前必须确认 4321 已释放，不能用 `dev:stop` 代替其他服务的停止操作。
- 启动、服务切换及 PR 合并后同步主目录时，都要实际请求 4321，结合进程工作目录和页面内容核对当前版本；合并收尾后的日常预览应来自主目录的最新 `main`。不能仅依据启动日志或 HTTP 200 判断成功。
