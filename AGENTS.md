# 项目协作规则

## 本地服务端口

- 本项目所有本地开发、预览和验收服务统一使用 `http://127.0.0.1:4321`，对用户提供的本地链接也必须使用 4321。
- 启动前检查 4321 的监听进程及所属工作目录。若为本项目旧预览，先停止旧进程，再启动当前版本；不要直接结束其他项目的进程。
- 禁止因端口占用自动递增或另开 4322、4323 等端口。无法安全释放 4321 时，说明占用情况，不得擅自换端口。
- 开发服务器、静态预览和本地公开服务共用 4321，按需切换运行，同一时间只保留一个入口。若需要同时运行多个服务，先确认如何统一入口，不得自行暴露额外端口。
- 启动后实际请求 4321，核对页面来自当前工作目录和最新改动，不能仅依据启动日志判断成功。
