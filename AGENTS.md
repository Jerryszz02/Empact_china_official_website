# 项目协作规则

## 唯一工作区

- 本项目唯一工作区为 `/Users/jerryszz/Desktop/实习/Empact/empactchinaOfficialWeb`。默认在此目录协作，不另建 worktree 或重复检出目录。
- 实现改动时仍从最新远端主分支创建任务分支；完成检查并合并 PR 后，将此目录切回最新 `main`，删除已完成任务的本地及远端分支。
- 切换分支前核对未提交文件；保留当前素材、业务数据和有效改动。启动服务前核对当前分支与提交，不能只检查页面是否返回 200。

## 统一开发预览

- 用户要求打开、重启或查看本地预览时，固定在上述唯一工作区根目录运行 `npm run dev`。日常预览不改用 `dev:cms`、`serve`、`serve:preview`、`serve:workspace`、直接运行 Astro 或临时自建服务。
- 本项目所有本地开发、预览和验收服务统一使用 `http://127.0.0.1:4321`，对用户提供的本地链接也必须使用 4321。
- 启动前检查 4321 的监听进程、所属工作目录、当前分支与提交。符合上述约定的开发服务可以复用；若为本项目旧目录或其他模式的服务，先停止该进程，再用 `npm run dev` 启动。不要直接结束其他项目的进程。
- 根目录的 `npm run dev:status`、`npm run dev:logs`、`npm run dev:stop` 仅用于查看和停止 `npm run dev` 启动的 Astro 开发预览。`npm run dev` 使用现有后台启动机制，验证成功后即可结束任务，不持续等待开发进程退出；日常代码修改由热更新处理，无需每次停止服务。
- 禁止因端口占用自动递增或另开 4322、4323 等端口。无法安全释放 4321 时，说明占用情况，不得擅自换端口。
- CMS、静态预览及生产服务命令仅用于明确要求的后台或发布工作，以及必要的自动化验证。测试需要独占 4321 时，先停本项目的开发预览；无论测试成功或失败，都清理测试服务并恢复根目录的 `npm run dev`，不得把测试服务留作用户的预览入口。
- 上述其他模式的服务通过其启动终端或测试清理流程停止；需要手动停止时，先核对监听 PID 及所属工作目录，再停止该服务进程。切回开发预览前必须确认 4321 已释放，不能用 `dev:stop` 代替其他服务的停止操作。
- 启动、服务切换及 PR 合并后同步主目录时，都要实际请求 4321，结合进程工作目录和页面内容核对当前版本；合并收尾后的预览应来自本目录的最新 `main`。不能仅依据启动日志或 HTTP 200 判断成功。
